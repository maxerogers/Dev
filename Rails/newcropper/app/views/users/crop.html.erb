<p id="notice"><%= notice %></p>

<% content_for(:head) do |v| %>
<%= javascript_include_tag :defaults %>

</script>

<% end %>

<p>
  <b>Name:</b>
  <%= @user.name %>
</p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>
 
<p> Original Image </p>
<hr />
<%= image_tag @user.avatar.url(:large), :id => "crop_box"%>
<br/>

<br/>
<p> Preview Image </p>
<hr />
<%= image_tag @user.avatar.url(:thumb) %>
<br/>

<%= form_for @user do |f| %>
  <% %w[x y w h].each do |attribute| %>
    <%= f.text_field "crop_#{attribute}" %><br/>
  <% end %>
  <div class="actions">
    <%= f.submit "Crop" %>
  </div>
<% end %>

<%= link_to 'Back', users_path %>
